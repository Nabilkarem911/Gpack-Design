const readline=require('readline');const bcrypt=require('bcryptjs');const {Pool}=require('pg');if(!process.env.DATABASE_URL)throw new Error('DATABASE_URL is required');const rl=readline.createInterface({input:process.stdin,output:process.stdout});const ask=q=>new Promise(r=>rl.question(q,r));(async()=>{const name=await ask('Name: '),login=await ask('Email or phone: '),password=await ask('Password (8+ chars): ');rl.close();if(password.length<8)throw new Error('Password must be at least 8 characters');const email=login.includes('@')?login.toLowerCase():null,phone=email?null:login.replace(/[^0-9+]/g,'');const p=new Pool({connectionString:process.env.DATABASE_URL});await p.query('INSERT INTO users(name,email,phone,password_hash,role) VALUES($1,$2,$3,$4,$5) ON CONFLICT DO NOTHING',[name,email,phone,bcrypt.hashSync(password,12),'ADMIN']);await p.end();console.log('Admin created if no matching unique login existed.')} )().catch(e=>{console.error(e.message);process.exit(1)});
